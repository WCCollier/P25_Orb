I'm building a proof-of-concept and pitch package for a job application take-home: ARC Edge Product Lead at Orb Aerospace. `design-document.md` in this directory is the complete design — read it in full before starting, it's the single source of truth for what to build and why. `CLAUDE.md` (already loaded) covers how I want you to work throughout; this message covers what to actually build.

**The goal:** a working proof-of-concept demo, its supporting design documentation, and a pitch script, all ready for me to present live to Orb Aerospace's team and defend under questioning. I need to be able to fully explain everything you build myself, so documentation quality matters as much as the code.

**What "done" looks like, in priority order. Build in this order. If you run low on effort or time, stop at the end of a fully-working tier rather than leaving every tier half-finished:**

1. **The core PoC — highest priority, this is what I actually present live, it needs to run flawlessly and reproducibly.** Two browser tabs per design-document.md Section 11: Tab 1 plays the scripted RF-event timeline; Tab 2 is the on-scene commander's Command Feed, driven live by Tab 1 via `BroadcastChannel`. Plain HTML/CSS/JS, no bundler, no build step — this needs to run by opening two tabs after starting one trivial local script. The detection/synthesis/alarm engine (Section 7) must be real, working code processing the classified event stream and applying the two-tier corroboration logic — not scripted UI output dressed up to look live. Use the demo script beats in Section 11 as the scripted timeline. Generate the classifications for that scripted sequence for real, by actually calling the Claude Haiku API during this build — real AI-produced labels, then cache the results into the script so the actual presentation has zero live-network dependency in its critical path. Separately, build an isolated "try it live" input elsewhere in the UI that does make a real Haiku API call, as a bonus/demo-of-the-underlying-tech moment that can fail safely without touching the main presentation.

   **Key handling — read design-document.md Section 8's "Key handling" note before building this.** An `ANTHROPIC_API_KEY` is available in `.env` in this directory. The "try it live" call must never embed that key in client-side JS. Stand up a minimal local proxy (a single small Python or Node script, no framework) that serves the static files and exposes one `/api/classify` endpoint holding the key server-side. This is the one piece of this "no build step" demo that needs a script running locally — document that clearly in the as-built summary and in whatever run instructions you leave me.

2. **Static Control Panel mockup (Section 8).** HTML/CSS only, not interactive, showing the config fields specified in the design document.

3. **Hardware and software design documentation (Sections 9 and 8, per the "Documentation" section of CLAUDE.md).** The hardware architecture spec for the P25 Orb module and the software PRDs for the Control Panel, Command Feed, and detection/synthesis engine.

4. **DF/triangulation standalone artifact (Section 10) — explicitly lowest priority, trim this first if you're short on effort.** A written explanation of the AOA approach, real runnable code (N position+bearing readings in, estimated location out), and a small standalone test of its own. Not integrated into the live demo — it's a separate "here's what we'd build next" artifact.

5. **Pitch script**, written for a live presentation to Texas DPS — our finalized customer of contract (Section 3) — voiced in the first person as if I'm delivering it myself. Cover: the problem, grounded in the scenario in Section 2; the differentiation, per Section 3's competitive landscape (Skymira/Etherstack relay what gets through — this detects and surfaces what never did); a walkthrough of the live demo; brief personal "why me" framing (I'll fill in specifics, just leave room for it); and a light roadmap mention (Section 12) that doesn't dilute focus on what's actually built. Written to be read aloud — a spoken narrative with clear section breaks, not a slide deck.

**Why this matters:** this is a real take-home for a real job application, reviewed by Orb Aerospace's hiring team, and I have to stand in front of them and defend every decision myself. The quality bar is "I can teach this back," not just "it runs."

**Boundaries:** design-document.md Section 13 lists what's still genuinely open — only Orb Aerospace's own hardware interface/enclosure specs remain unanswered at this point. Treat those as open assumptions the way the design document does, don't invent specifics to paper over them.
